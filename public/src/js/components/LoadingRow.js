import React from 'react';

const LoadingRow = ({}) => (
  <div className="expense-row">
    Loading...
    <span className="pull-right">
      spin
    </span>
  </div>
);

export default LoadingRow;
